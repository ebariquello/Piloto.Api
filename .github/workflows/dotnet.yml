name: .NET

on:
  push:
    branches: [ "master", "dev" ]
  pull_request:
    branches: [ "master", "dev" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 8.0.x
    - name: Install ReportGenerator
      run: dotnet tool install --global dotnet-reportgenerator-globaltool
    - name: Restore dependencies
      run: dotnet restore Piloto.Api.sln
    - name: Build
      run: dotnet build Piloto.Api.sln --no-restore
    - name: Test with coverage
      run: dotnet test Piloto.Api.sln --no-build --verbosity normal /p:CollectCoverage=true /p:CoverletOutputFormat=cobertura /p:CoverletOutput=./TestResults/
    - name: Generate coverage report
      run: reportgenerator -reports:./TestResults/coverage.cobertura.xml -targetdir:./TestResults/CoverageReport
    - name: Check coverage threshold
      run: |
        coverage=$(grep -oP '(?<=<coverage line-rate=")[^"]*' ./TestResults/CoverageReport/index.html)
        coverage=$(echo "$coverage * 100" | bc)
        if (( $(echo "$coverage < 65" | bc -l) )); then
          echo "Code coverage ($coverage%) is below the threshold of 65%."
          exit 1
        fi
    - name: Upload coverage report
      uses: actions/upload-artifact@v3
      with:
        name: code-coverage-report
        path: ./TestResults/CoverageReport